# RUNBOOK — Workflow Index

| Scenario | Procedure |
|----------|-----------|
| Any working session (open, log, close) | [runbook/session.md](runbook/session.md) |

New workflows are added as separate `runbook/` files when a procedure is repeated enough to
need one — see GROWTH.md.
