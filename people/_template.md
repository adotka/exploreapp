# <Полное имя>

**Type:** person | collective
**Status:** active

<!-- Файл — источник истины о человеке или коллективе (оркестр, хор, ансамбль). Создавайте
     КОПИРОВАНИЕМ этого шаблона. Имя файла: people/<латинская транслитерация имени>.md.

     КОГДА заводить файл:
       - Type: person, встречен ПОВТОРНО (уже отмечен как «повторный» на /uchastniki/) — общий
         случай, разовое+по мере обнаружения обогащение, не обязательное поле для каждого
         человека.
       - Type: person, но роль — «автор» (композитор/драматург исходного произведения) —
         заводится ВСЕГДА, даже при единственной встрече: автор — не участник конкретного
         показа, а точка входа в произведение (per P-shared-facts-own-entity), и достаточно
         важен, чтобы не оставаться без описания только потому, что мы видели его работу один
         раз.
       - Type: collective — постоянно действующий коллектив-исполнитель (оркестр, хор,
         ансамбль), упомянутый в ## Состав отдельной строкой с ролью-типом коллектива
         («Оркестр», «Хор», «Ансамбль») и названием коллектива как именем; заводится всегда,
         не только при повторной встрече, по той же логике, что и авторы.

     ИДЕНТИЧНОСТЬ: **Имя** здесь должно ПОЛНОСТЬЮ СОВПАДАТЬ (точная строка) с тем, как
     человек/коллектив записан в items/*.md — иначе генератор сайта не свяжет профиль с
     карточкой.

     Фото: миниатюра, не полноразмерное фото — только чтобы узнать человека/коллектив
     (ориентир ~150–200px по широкой стороне). Необязательно — можно оставить пустым (особенно
     для авторов/коллективов, где короткое текстовое описание важнее фото). Кладите файл в
     people/photos/, путь указывайте относительно корня репозитория. Источник — по возможности
     официальная страница театра/коллектива (наименьший риск); для исторических фигур
     (композиторы и т.п.) — общественное достояние.

     МАШИННЫЙ ФОРМАТ (его читает helpers/build_site.py): поле — - **Ключ:** значение. -->

## Facts

- **Имя:** <точно как в items/*.md>
- **Фото:** <people/photos/<slug>.jpg или пусто, если не найдено/не нужно>
- **Коротко:** <1 короткое предложение — для всплывающей подсказки при наведении>
- **Био:** <несколько предложений — для карточки персоны/коллектива>
- **Источник:** <ссылка на страницу-источник био/фото>

## History

- YYYY-MM-DD — создано при обогащении данных (sessions/YYYY-MM-DD_<topic>.md)
