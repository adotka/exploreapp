# <Полное имя>

**Type:** person
**Status:** active

<!-- Файл — источник истины о человеке, которого мы встречаем ПОВТОРНО в спектаклях
     (дирижёры, солисты, постановщики...). Создавайте КОПИРОВАНИЕМ этого шаблона, только
     для людей, уже отмеченных как «повторные» на /lyudi/ (это разовое+по мере
     обнаружения обогащение, не обязательное поле для каждого человека). Имя файла:
     people/<латинская транслитерация имени>.md.

     ИДЕНТИЧНОСТЬ: **Имя** здесь должно ПОЛНОСТЬЮ СОВПАДАТЬ (точная строка) с тем, как
     человек записан в items/*.md — иначе генератор сайта не свяжет профиль с карточкой.

     Фото: миниатюра, не полноразмерное фото — только чтобы узнать человека
     (ориентир ~150–200px по широкой стороне). Кладите файл в people/photos/, путь
     указывайте относительно корня репозитория. Источник — по возможности официальная
     страница театра/коллектива (наименьший риск); для исторических фигур (композиторы
     и т.п.) — общественное достояние.

     МАШИННЫЙ ФОРМАТ (его читает helpers/build_site.py): поле — - **Ключ:** значение. -->

## Facts

- **Имя:** <точно как в items/*.md>
- **Фото:** <people/photos/<slug>.jpg или пусто, если не найдено>
- **Коротко:** <1 короткое предложение — для всплывающей подсказки при наведении>
- **Био:** <несколько предложений — для карточки персоны>
- **Источник:** <ссылка на страницу-источник био/фото>

## History

- YYYY-MM-DD — создано при обогащении данных о повторных людях (sessions/YYYY-MM-DD_<topic>.md)
